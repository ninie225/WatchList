<div class="container text-secondary-emphasis">
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="{{ path('app_accueil') }}">
                <img src="{{ asset('uploads/logo/logo.png') }}" alt="Logo WatchList" height="70">
            </a>
            <a class="navbar-brand" href="{{ path('app_accueil') }}" class="fw-bold">WatchList</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_accueil') }}">HomePage</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_watched') }}">Movies seen</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href=" {{ path('app_toWatch') }}">Movies to watch</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_top5') }}">Top 5 movies</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
</div>